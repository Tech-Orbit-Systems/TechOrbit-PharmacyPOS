const {createProductTemplate,parseWorkbookBuffer,HEADERS}=require("../infrastructure/import/product-import-files");describe("Excel product import files",()=>{test("generates a documented template that round-trips without losing leading-zero barcode",async()=>{const buffer=await createProductTemplate();const rows=await parseWorkbookBuffer(buffer);expect(buffer.length).toBeGreaterThan(1000);expect(rows[0].barcode).toBe("0012345678901");expect(Object.keys(rows[0])).toEqual(HEADERS);});});
