const {SqliteCounterClient}=require("../assets/js/sqlite-counter-client");
describe("SQLite counter client adapter",()=>{test("is disabled unless explicitly enabled",()=>{expect(SqliteCounterClient.isEnabled({getItem:()=>null})).toBe(false);expect(SqliteCounterClient.isEnabled({getItem:()=>"true"})).toBe(true);expect(SqliteCounterClient.isEnabled({getItem:()=>true})).toBe(true);});test("encodes barcode and surfaces safe API errors",async()=>{const calls=[];const client=new SqliteCounterClient({baseUrl:"http://local/api/v2/",fetchImpl:async(url)=>{calls.push(url);return{ok:true,json:async()=>({id:1})};}});expect(await client.barcodeLookup("00 12/3")).toEqual({id:1});expect(calls[0]).toContain("00%2012%2F3");const failing=new SqliteCounterClient({fetchImpl:async()=>({ok:false,status:404,json:async()=>({error:"PRODUCT_NOT_FOUND",message:"Missing"})})});await expect(failing.receipt(9)).rejects.toMatchObject({message:"Missing",code:"PRODUCT_NOT_FOUND",status:404});});});
