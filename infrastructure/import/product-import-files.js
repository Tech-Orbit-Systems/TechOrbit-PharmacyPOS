const ExcelJS=require("exceljs");
const HEADERS=["sku","barcode","name","generic_name","manufacturer","category","product_type","dosage_form","strength","pack_description","base_unit","units_per_strip","strips_per_box","base_sale_price","strip_sale_price","box_sale_price","minimum_stock","reorder_level","prescription_required","controlled_medicine","gst_percent","tax_status","active","notes"];
async function parseWorkbookBuffer(buffer){const workbook=new ExcelJS.Workbook();await workbook.xlsx.load(buffer);const sheet=workbook.worksheets[0];if(!sheet)throw new Error("Workbook has no worksheet");const headers=sheet.getRow(1).values.slice(1).map(v=>String(v||"").trim().toLowerCase());const rows=[];sheet.eachRow((row,n)=>{if(n===1)return;const record={};headers.forEach((header,index)=>{if(header)record[header]=row.getCell(index+1).value;});if(Object.values(record).some(v=>v!==null&&v!==undefined&&String(v).trim()!==""))rows.push(record);});return rows;}
async function createProductTemplate(){const workbook=new ExcelJS.Workbook();const sheet=workbook.addWorksheet("Products");sheet.addRow(HEADERS);sheet.addRow(["PAN-500","0012345678901","Panadol 500mg","Paracetamol","GSK","Analgesic","medicine","tablet","500mg","10 strips x 10 tablets","tablet",10,10,12,110,1000,50,100,"no","no",0,"exempt","yes","Example row - replace or delete"]);sheet.views=[{state:"frozen",ySplit:1}];sheet.getRow(1).font={bold:true};sheet.columns.forEach(column=>column.width=20);return Buffer.from(await workbook.xlsx.writeBuffer());}
module.exports={HEADERS,parseWorkbookBuffer,createProductTemplate};
