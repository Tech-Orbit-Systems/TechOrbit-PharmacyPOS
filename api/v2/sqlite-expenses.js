const express=require("express");const {ExpensesService}=require("../../infrastructure/sqlite/services/expenses");function createExpensesRouter({getDatabase}){const r=express.Router();r.post("/",(req,res)=>{try{const x=new ExpensesService(getDatabase()).post(req.body);res.status(x.idempotent?200:201).json(x);}catch(e){res.status(422).json({error:"EXPENSE_POST_FAILED",message:e.message});}});r.post("/:id/void",(req,res)=>{try{res.json(new ExpensesService(getDatabase()).void({...req.body,expenseId:Number(req.params.id)}));}catch(e){res.status(422).json({error:"EXPENSE_VOID_FAILED",message:e.message});}});r.get("/categories",(req,res)=>res.json(getDatabase().prepare("SELECT * FROM ExpenseCategories WHERE active=1 ORDER BY name").all()));return r;}module.exports={createExpensesRouter};
